# Optional in demo deployment (Vercel): if omitted, frontend runs with built-in demo data.
VITE_API_URL=http://localhost:8000

# Backward-compatible alias used by local setups.
VITE_API_BASE_URL=http://localhost:8000
